# Server Configuration
PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# Database Configuration
DATABASE_URL=

# JWT Secret (CHANGE THIS in production — use a strong random key)
JWT_SECRET=

# Arcjet (optional — remove if not using Arcjet security)
ARCJET_KEY=
